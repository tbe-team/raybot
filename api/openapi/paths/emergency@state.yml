get:
  summary: Get the emergency state of the robot
  operationId: getEmergencyState
  description: Get the emergency state of the robot
  tags:
    - emergency
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            $ref: "../components/schemas/emergency.yml#/EmergencyState"
    "400":
      description: Bad Request
      content:
        application/json:
          schema:
            $ref: "../components/schemas/error.yml#/ErrorResponse"
